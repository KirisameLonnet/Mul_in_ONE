# direnv configuration for Mul-in-One
# shellcheck shell=bash

if [ ! -d .venv ]; then
  echo "[direnv] creating uv virtualenv at .venv"
  uv venv .venv
fi

if [ -f .venv/bin/activate ]; then
  source .venv/bin/activate
fi

source_env_if_exists .envrc.local
